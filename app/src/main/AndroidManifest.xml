<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.m.hadiservices">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/splash_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/splash_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Sign_Up_Form_Activity" />
        <activity android:name=".PickUpRequestForm" />
        <activity android:name=".CourierServiceBokingForm" />
        <activity android:name=".CourierServiceStatusUpdateForm" />
        <activity android:name=".EducatinoalAndJobServicesForm" />
        <activity android:name=".TravelAndToursServicesForm" />
        <activity android:name=".CashUpdateForm" />
        <activity android:name=".CourierServiceFinalStatusForm" />
        <activity android:name=".Home" />
        <activity android:name=".NewHome" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerHome"></activity>
        <activity android:name=".GetSignature"></activity>
    </application>

</manifest>